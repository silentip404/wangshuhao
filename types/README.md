# Types 目录说明

本目录包含项目中手动编写的类型声明文件，用于为非 TypeScript 的依赖提供类型支持。

## 📁 目录结构

```
types/
├── README.md                      # 本说明文档
└── eslint-config-next.d.ts        # eslint-config-next 包的类型声明
```

## 📝 类型声明说明

### `eslint-config-next.d.ts`

为 `eslint-config-next` 包提供 TypeScript 类型声明，声明了 `core-web-vitals` 和 `typescript` 配置模块的类型。
